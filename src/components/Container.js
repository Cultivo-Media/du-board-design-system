import styled from 'styled-components';

/**
 * Container
 *
 * Styled Component
 *
 * A main wrapper for the application that sets a max width and automatically aligns content
 *  appropriately based on that width.
 */
const Container = styled.div`
  margin: 0 auto;
  max-width: 1200px;
  width: 90%;
`;

export default Container;
